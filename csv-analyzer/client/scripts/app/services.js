var app = angular.module('CSVApp');

app.factory(
    'FileUploadService',

    function(){

        var FileUploadService = {};

        FileUploadService.start_upload = function(file) {

        };

        FileUploadService.pause_upload = function(file) {

        };

        FileUploadService.resume_upload = function(file) {

        };

        FileUploadService.cancel_upload = function(file) {

        };

        return FileUploadService;
    }
);
